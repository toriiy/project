import React from 'react';
import Filter from "../components/filter/Filter";

const FilterPage = () => {
    return (
        <div>
            <Filter/>
        </div>
    );
};

export default FilterPage;