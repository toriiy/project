import React from 'react';
import Contacts from "../components/contacts/Contacts";

const ContactPage = () => {
    return (
        <div>
            <Contacts/>
        </div>
    );
};

export default ContactPage;